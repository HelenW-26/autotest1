<?xml version="1.0" encoding="UTF-8" standalone="no"?><!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="app api create user" configfailurepolicy="continue">

	<!-- 对应Jenkins Job  QA share-App api registrations-->
	
	<parameter name="Brand" value="${Brand}"></parameter>     
  	<parameter name="Regulator" value="${Regulator}"></parameter>
  	<parameter name="TestEnv" value="${Environment}"></parameter>
  	<parameter name="APPURL" value="${app_url}"></parameter>
    <parameter name="IBCode" value="${affid}"></parameter>
    <parameter name="RAFCode" value="${rafcode}"></parameter>
    <parameter name="Currency" value ="${currency}"></parameter>
    <parameter name="AccountType" value="${accountType}"></parameter>
    <parameter name="Platform" value = "${platform}"></parameter>
    <parameter name="Country" value="${Country}"></parameter>
  
  <test name="app api create user">
    <classes>
     <class name="newcrm.testcases.app.APPRegression">
   		 	<methods>
   		 		<include name="registration"></include>
   		 	</methods>
   		 </class>
    </classes>
  </test>
</suite><!-- Suite -->