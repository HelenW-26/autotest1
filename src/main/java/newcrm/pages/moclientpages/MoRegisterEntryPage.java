package newcrm.pages.moclientpages;

import newcrm.pages.clientpages.register.RegisterEntryPage;
import org.openqa.selenium.WebDriver;

public class MoRegisterEntryPage extends RegisterEntryPage{
    public MoRegisterEntryPage(WebDriver driver) {
        super(driver);
    }
}
